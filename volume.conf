# claudes-plan master volume.
# 0.0 = mute, 1.0 = full. (afplay also accepts values > 1.0 to amplify.)
# Edit the number below; changes take effect on the next sound.
# Override per-session with the CLAUDESPLAN_VOLUME env var.
1.0
